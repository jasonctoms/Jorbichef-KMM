CREATE TABLE tag (
  id TEXT NOT NULL PRIMARY KEY,
  name TEXT NOT NULL,
  resourceId TEXT
);

selectAllTags:
SELECT * FROM tag;

insertOrReplace:
REPLACE INTO tag (id, name, resourceId) VALUES (?, ?, ?);