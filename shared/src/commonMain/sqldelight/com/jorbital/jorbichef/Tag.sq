CREATE TABLE tag (
  id TEXT NOT NULL PRIMARY KEY,
  name TEXT NOT NULL,
  resourceId TEXT
);

selectAll:
SELECT * FROM tag;

insertOrReplace:
REPLACE INTO tag (id, name, resourceId) VALUES (?, ?, ?);